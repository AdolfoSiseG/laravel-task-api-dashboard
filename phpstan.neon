includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app
        - database
        - routes
        - resources/views/livewire

    # Climbing target: raise toward level 8 as the codebase stabilises.
    level: 6
